# 多式联运稳定匹配系统 - 前端需求文档

## 项目概述
基于React + 百度地图API技术栈开发多式联运稳定匹配系统的前端界面，实现运输路线可视化、货物信息管理和匹配结果展示三大核心功能模块。

## 技术栈要求
- **框架**: React 18+ (函数组件 + Hooks)
- **地图服务**: 百度地图JavaScript API
- **编程语言**: JavaScript (不使用TypeScript)
- **状态管理**: React Context + useReducer
- **UI组件库**: Ant Design 5.0+
- **HTTP客户端**: Axios
- **路由**: React Router 6+
- **构建工具**: Vite

## 核心功能需求

### 功能一：原始路线查看界面

#### 界面布局
```
┌─────────────────────────────────────────────────────────────┐
│  多式联运稳定匹配系统 - 原始路线查看                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ 百度地图模式  │  │ SVG矢量模式  │  │ 路线筛选     │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────┤
│                        地图区域                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                                                       │  │
│  │                    地图容器                           │  │
│  │                                                       │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  路线列表表格                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 路线ID │ 起点城市 │ 终点城市 │ 总成本 │ 总时间 │ 操作 │  │
│  │   1    │ 重庆     │ 海防     │ 16305  │ 18.0   │ 查看 │  │
│  │   2    │ 重庆     │ 北部湾   │ 9822   │ 24.0   │ 查看 │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 功能需求
1. **双地图模式切换**
   - 百度地图模式：使用百度地图JavaScript API
   - SVG矢量模式：使用SVG矢量图形渲染
   - 默认显示百度地图模式

2. **路线可视化**
   - 在地图上完整显示所有运输路线
   - 每条路线使用不同颜色标识
   - 路线起点显示绿色标记，终点显示红色标记
   - 鼠标悬停显示路线基本信息（路线ID、起点、终点）

3. **路线交互功能**
   - 点击任意路线高亮显示
   - 弹出信息窗口显示路线详情
   - 支持路线轨迹动画播放

4. **路线筛选**
   - 按起点城市筛选
   - 按终点城市筛选
   - 按成本范围筛选
   - 按时间范围筛选

5. **数据表格展示**
   - 显示所有路线基本信息
   - 支持分页（每页10条）
   - 支持按列排序
   - 支持关键字搜索
   - 点击"查看"按钮显示路线详情

#### 技术实现要点
```javascript
// 百度地图初始化
const initBaiduMap = () => {
  const map = new BMap.Map("mapContainer");
  map.centerAndZoom(new BMap.Point(106.5, 26.0), 7);
  map.enableScrollWheelZoom(true);
  return map;
};

// 路线绘制
const drawRoute = (map, routeData) => {
  const points = routeData.node_details.map(node => 
    new BMap.Point(node.longitude, node.latitude)
  );
  
  const polyline = new BMap.Polyline(points, {
    strokeColor: getRouteColor(routeData.route_id),
    strokeWeight: 4,
    strokeOpacity: 0.8
  });
  
  map.addOverlay(polyline);
  
  // 添加点击事件
  polyline.addEventListener("click", () => {
    showRouteDetails(routeData);
  });
};
```

### 功能二：货物信息查看界面

#### 界面布局
```
┌─────────────────────────────────────────────────────────────┐
│  多式联运稳定匹配系统 - 货物信息管理                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ 添加货物     │  │ 批量导入     │  │ 导出数据     │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  搜索筛选区域                                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ 起点城市 ▼   │  │ 终点城市 ▼   │  │ 搜索        │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  货物数据表格                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 货物ID │ 起点 │ 终点 │ 需求 │ 重量 │ 体积 │ 时间价值 │  │
│  │   1    │ 重庆 │ 海防 │  49  │ 980  │1617  │   100    │  │
│  │   2    │ 重庆 │ 北部湾│  35  │ 700  │1155  │   120    │  │
│  │   3    │ 重庆 │ 贵阳 │  28  │ 560  │ 924  │    80    │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  分页控制: [首页] [上一页] [1][2][3][4][5] [下一页] [尾页]      │
└─────────────────────────────────────────────────────────────┘
```

#### 功能需求
1. **数据表格展示**
   - 显示所有货物的完整信息
   - 支持列排序（ID、重量、体积、时间价值）
   - 支持分页显示（每页20条）
   - 支持表格列宽调整

2. **搜索筛选功能**
   - 按起点城市下拉筛选
   - 按终点城市下拉筛选
   - 按货物ID精确搜索
   - 按关键字模糊搜索

3. **货物操作功能**
   - 查看货物详情（模态框）
   - 编辑货物信息（可修改重量、体积、时间价值）
   - 删除货物（确认对话框）
   - 批量删除（多选+确认）

4. **数据导入导出**
   - 支持Excel文件导入
   - 支持CSV格式导出
   - 支持当前筛选结果导出

5. **实时统计信息**
   - 显示货物总数量
   - 显示总重量和总体积
   - 显示平均时间价值

#### 技术实现要点
```javascript
// 货物数据获取
const fetchShipments = async (filters) => {
  try {
    const response = await axios.get('/api/shipments', {
      params: {
        origin: filters.origin,
        destination: filters.destination,
        page: filters.page,
        pageSize: filters.pageSize
      }
    });
    return response.data;
  } catch (error) {
    message.error('获取货物数据失败');
    return null;
  }
};

// 表格列配置
const columns = [
  {
    title: '货物ID',
    dataIndex: 'shipment_id',
    key: 'shipment_id',
    sorter: (a, b) => a.shipment_id - b.shipment_id,
  },
  {
    title: '起点城市',
    dataIndex: 'origin_city',
    key: 'origin_city',
    filters: cityFilters,
    onFilter: (value, record) => record.origin_city === value,
  },
  {
    title: '终点城市',
    dataIndex: 'destination_city',
    key: 'destination_city',
  },
  {
    title: '重量(kg)',
    dataIndex: 'weight',
    key: 'weight',
    sorter: (a, b) => a.weight - b.weight,
  }
];
```

### 功能三：匹配结果查看界面

#### 界面布局
```
┌─────────────────────────────────────────────────────────────┐
│  多式联运稳定匹配系统 - 匹配结果查看                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ 地图视图     │  │ 表格视图     │  │ 统计分析     │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  匹配统计信息                                                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ 总匹配率: 72.5% │  │ 稳定性: 稳定    │  │ 匹配数量: 81    │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  地图区域 + 匹配结果表格                                     │
│  ┌───────────────────────┐  ┌─────────────────────────────┐  │
│  │                       │  │ 货物ID │ 分配路线 │ 状态  │  │
│  │      百度地图         │  │   1    │   10     │ 已匹配│  │
│  │                       │  │   2    │   Self   │ 自营  │  │
│  │  显示匹配路线和货物   │  │   3    │   15     │ 已匹配│  │
│  │                       │  │   4    │   Self   │ 自营  │  │
│  └───────────────────────┘  └─────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  路线详情面板（点击路线时显示）                                │
│  ┌───────────────────────────────────────────────────────┐    │
│  │ 路线1: 重庆→贵阳→北部湾→海防                          │    │
│  │ 匹配货物: [6, 8, 11, 12, 13]                        │    │
│  │ 匹配率: 72.5% | 总成本: 16305 | 时间: 18小时        │    │
│  └───────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### 功能需求
1. **双视图模式**
   - 地图视图：在百度地图上显示匹配结果
   - 表格视图：纯表格形式展示匹配数据
   - 支持视图间无缝切换

2. **地图可视化功能**
   - 显示所有匹配路线（彩色线条）
   - 显示未匹配货物（灰色标记）
   - 显示已匹配货物（绿色标记）
   - 路线点击显示匹配的货物列表
   - 货物点击显示详细信息和分配路线

3. **交互功能**
   - 点击路线高亮显示并展示货物列表
   - 点击货物显示分配详情
   - 支持路线和货物的关联显示
   - 鼠标悬停显示基本信息

4. **统计分析**
   - 整体匹配率统计
   - 匹配稳定性状态
   - 各路线匹配情况分析
   - 自营货物比例统计

5. **数据导出**
   - 支持匹配结果导出Excel
   - 支持路线分配详情导出
   - 支持统计分析报告导出

#### 技术实现要点
```javascript
// 匹配结果地图展示
const displayMatchingResults = (map, matchingData) => {
  // 绘制匹配路线
  matchingData.routes.forEach(route => {
    drawMatchingRoute(map, route);
  });
  
  // 标记货物
  matchingData.shipments.forEach(shipment => {
    if (shipment.assigned_route === "Self") {
      // 自营货物 - 灰色标记
      addShipmentMarker(map, shipment, 'gray');
    } else {
      // 已匹配货物 - 绿色标记
      addShipmentMarker(map, shipment, 'green');
    }
  });
};

// 路线点击事件处理
const handleRouteClick = (routeData) => {
  // 高亮路线
  highlightRoute(routeData.route_id);
  
  // 获取匹配的货物
  fetchRouteShipments(routeData.route_id).then(shipments => {
    setSelectedShipments(shipments);
    setShowDetailPanel(true);
  });
};
```

## 通用功能需求

### 1. 地图功能
- **缩放控制**: 支持鼠标滚轮和按钮缩放
- **拖拽移动**: 支持地图拖拽移动
- **全屏显示**: 支持地图全屏模式
- **截图导出**: 支持地图截图保存

### 2. 数据加载
- **懒加载**: 大数据量时分批加载
- **加载状态**: 显示加载动画和进度
- **错误处理**: 网络错误重试机制
- **缓存机制**: 本地缓存常用数据

### 3. 响应式设计
- **桌面端**: 1920x1080分辨率优化
- **平板端**: 768px以上宽度适配
- **移动端**: 基础功能支持

### 4. 性能优化
- **虚拟滚动**: 大数据表格使用虚拟滚动
- **地图瓦片**: 百度地图瓦片缓存
- **防抖节流**: 搜索和筛选防抖处理
- **组件懒加载**: 路由级代码分割

### 5. 用户体验
- **加载动画**: 数据加载时的友好提示
- **操作反馈**: 所有操作都有即时反馈
- **键盘导航**: 支持Tab键导航
- **无障碍**: 基本的无障碍支持

## 项目结构建议
```
src/
├── components/          # 通用组件
│   ├── MapViewer/      # 地图查看器组件
│   ├── DataTable/      # 数据表格组件
│   ├── FilterPanel/      # 筛选面板组件
│   └── Charts/          # 图表组件
├── pages/              # 页面组件
│   ├── RoutesView/      # 路线查看页面
│   ├── ShipmentsView/   # 货物管理页面
│   └── MatchingView/   # 匹配结果页面
├── services/           # API服务
│   ├── api.js          # API请求封装
│   └── mapService.js   # 地图相关服务
├── hooks/              # 自定义Hooks
│   ├── useMap.js       # 地图操作Hook
│   └── useData.js      # 数据管理Hook
├── utils/              # 工具函数
│   ├── formatters.js   # 数据格式化
│   └── constants.js    # 常量定义
└── assets/             # 静态资源
    ├── styles/         # 样式文件
    └── images/         # 图片资源
```

## 开发里程碑

### 第一阶段（基础框架）
- [ ] 项目初始化和依赖安装
- [ ] 基础布局组件开发
- [ ] API服务封装
- [ ] 路由配置

### 第二阶段（核心功能）
- [ ] 货物信息管理界面
- [ ] 基础数据表格组件
- [ ] 简单筛选功能

### 第三阶段（地图集成）
- [ ] 百度地图API集成
- [ ] SVG矢量地图实现
- [ ] 路线可视化展示

### 第四阶段（高级功能）
- [ ] 匹配结果展示
- [ ] 交互功能完善
- [ ] 数据导出功能

### 第五阶段（优化完善）
- [ ] 性能优化
- [ ] 响应式适配
- [ ] 用户体验优化